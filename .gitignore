# Generated by cargo
#
# To learn more, see Cargo's global config file at `~/.cargo/config`
# and the global gitignore file at `~/.config/git/ignore`.

# Ignore compiled artifacts
/target/

# Ignore `Cargo.lock` if you are building a library, but not if you are
# building an application. For this project, we are building an application,

# so we will not ignore it.
# Cargo.lock

# Other common ignores
*.rs.bk
*.log
.DS_Store
